# ============================================================================
# Configuración de Bases de Datos - SentimentInsightUAM_SA
# ============================================================================
# IMPORTANTE: Este proyecto se conecta a las MISMAS bases de datos que
# SentimentInsightUAM (el proyecto de scraping). Asegúrate de que Docker
# esté corriendo con: docker-compose up -d (en el proyecto principal)
# ============================================================================

# ============================================================================
# PostgreSQL (Datos Estructurados)
# ============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=sentiment_uam_db
POSTGRES_USER=sentiment_admin
POSTGRES_PASSWORD=dev_password_2024

# URL de conexión (asyncpg para SQLAlchemy)
DATABASE_URL=postgresql+asyncpg://sentiment_admin:dev_password_2024@localhost:5432/sentiment_uam_db

# ============================================================================
# MongoDB (Opiniones Textuales y Análisis)
# ============================================================================
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=sentiment_uam_nlp
MONGO_USER=sentiment_admin
MONGO_PASSWORD=dev_password_2024

# URL de conexión MongoDB
MONGO_URL=mongodb://sentiment_admin:dev_password_2024@localhost:27017/sentiment_uam_nlp?authSource=sentiment_uam_nlp

# ============================================================================
# Configuración de Modelo BERT
# ============================================================================
# Modelo BERT para análisis de sentimiento en español
# Opciones recomendadas:
# - dccuchile/bert-base-spanish-wwm-cased (Mejor para español general)
# - PlanTL-GOB-ES/roberta-base-bne (Variante RoBERTa española)
# - mrm8488/distilroberta-finetuned-sentiment-spa (Optimizado para sentimiento)
BERT_MODEL_NAME=dccuchile/bert-base-spanish-wwm-cased

# Ruta local para cache del modelo (descargado de HuggingFace)
MODEL_CACHE_DIR=./models/cache

# Dispositivo de cómputo (cpu, cuda, mps para Apple Silicon)
DEVICE=cpu

# Tamaño de batch para procesamiento
BATCH_SIZE=8

# ============================================================================
# Configuración de Análisis
# ============================================================================
# Límite de opiniones a procesar por ejecución (-1 para sin límite)
MAX_OPINIONS_PER_RUN=-1

# Umbral de confianza para clasificación (0.0 a 1.0)
CONFIDENCE_THRESHOLD=0.7

# Habilitar cache de resultados (evita re-analizar textos idénticos)
ENABLE_CACHE=true

# ============================================================================
# Logging y Debug
# ============================================================================
LOG_LEVEL=INFO
DEBUG=false
